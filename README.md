# systemdesign

The Project: "Fleet-AI" Asset Tracker

Day 1: Set up the FastAPI backend to serve mock GPS coordinates and AI-generated "Maintenance Alerts" (using logic similar to your Spare Part Predictor ).

Day 2: Build the React dashboard with a live map that updates when you click a "Refresh" button or on a timer.